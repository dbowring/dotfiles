# Path to zsh binary
export ZSH_FULL_NAME=$(which $ZSH_NAME)
# Path to tmux binary
export TMUX_FULL_NAME=$(which tmux)

# Alacritty options
export ALACRITTY_FONT_SIZE=11
export TMUX_SESSION_NAME=alacritty
